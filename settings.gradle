pluginManagement {
    plugins {
        id "com.github.johnrengelman.shadow" version "7.1.2"
        id "java"
    }
}

rootProject.name = 'justrickycore'
include 'api'
include 'plugin'

findProject(":api")?.setName("justrickycore-api")

